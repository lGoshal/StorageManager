<UserControl x:Class="StorageManager.Views.AddressesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StorageManager"
             xmlns:converters="clr-namespace:StorageManager.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">

    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,5,0,10"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="0,5,0,10"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" 
                    Background="#1565C0" 
                    CornerRadius="8"
                    Padding="15"
                    Margin="0,0,0,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <TextBlock Text="📍" FontSize="24" Margin="0,0,10,0"/>
                        <TextBlock Text="Адреса" 
                                   FontSize="18" 
                                   FontWeight="Bold"
                                   Foreground="White"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <Button Content="➕ Новый адрес" 
                                Command="{Binding AddAddressCommand}"
                                Background="#28a745"
                                Foreground="White"/>

                        <Button Content="🔄 Обновить" 
                                Command="{Binding LoadAddressesCommand}"
                                Background="#6c757d"
                                Foreground="White"
                                Margin="10,0,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="450"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" 
                        Margin="0,0,10,0"
                        BorderBrush="#E0E0E0" 
                        BorderThickness="1" 
                        CornerRadius="8"
                        DataContext="{Binding DataContext, RelativeSource={RelativeSource AncestorType=UserControl}}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" 
                                    Orientation="Horizontal" 
                                    Margin="15,15,15,10">
                            <StackPanel Width="300" Margin="0,0,10,0">
                                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                         Background="White"
                                         BorderBrush="#E0E0E0"
                                         Padding="8"/>
                                <TextBlock Text="Поиск адресов..." 
                                           Margin="10,-55,0,0"
                                           VerticalAlignment="Center"
                                           Foreground="#999"
                                           IsHitTestVisible="False">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding SearchText}" Value="">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding SearchText}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </StackPanel>
                        </StackPanel>

                        <DataGrid Grid.Row="1"
                                  ItemsSource="{Binding FilteredAddresses}"
                                  SelectedItem="{Binding SelectedAddress}"
                                  AutoGenerateColumns="False"
                                  IsReadOnly="True"
                                  HeadersVisibility="Column"
                                  BorderThickness="0"
                                  SelectionMode="Single"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  CanUserResizeColumns="True"
                                  CanUserSortColumns="True"
                                  AlternatingRowBackground="#F8F9FA"
                                  Margin="15,0,15,15">

                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID" 
                                                    Binding="{Binding AddressId}" 
                                                    Width="50"/>

                                <DataGridTextColumn Header="Адрес" 
                                                    Binding="{Binding AddressView}" 
                                                    Width="400"/>

                                <DataGridTextColumn Header="Страна" 
                                                    Binding="{Binding CountryName}" 
                                                    Width="120"/>

                                <DataGridTextColumn Header="Город" 
                                                    Binding="{Binding CityName}" 
                                                    Width="150"/>

                                <DataGridTextColumn Header="Дом" 
                                                    Binding="{Binding HouseNumber}" 
                                                    Width="80"/>

                                <DataGridTemplateColumn Header="Действия" Width="120">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <Button Content="✏️" 
                                                        Command="{Binding DataContext.EditAddressCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                        CommandParameter="{Binding}"
                                                        ToolTip="Редактировать"
                                                        Background="Transparent"
                                                        BorderThickness="0"
                                                        Padding="5"
                                                        Cursor="Hand"/>

                                                <Button Content="🗑️" 
                                                        Command="{Binding DataContext.DeleteAddressCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                        CommandParameter="{Binding}"
                                                        ToolTip="Удалить"
                                                        Background="Transparent"
                                                        BorderThickness="0"
                                                        Padding="5"
                                                        Cursor="Hand"
                                                        Margin="5,0,0,0"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>

                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow">
                                    <Setter Property="Cursor" Value="Hand"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#E3F2FD"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.RowStyle>
                        </DataGrid>
                    </Grid>
                </Border>

                <Border Grid.Column="1" 
                        BorderBrush="#E0E0E0" 
                        BorderThickness="1" 
                        CornerRadius="8"
                        Padding="20">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <TextBlock Text="{Binding FormTitle}" 
                                       FontSize="16" 
                                       FontWeight="Bold"
                                       Margin="0,0,0,20"/>

                            <StackPanel>
                                <TextBlock Text="Иерархия адреса" 
                                           FontWeight="SemiBold"
                                           FontSize="14"
                                           Margin="0,0,0,10"/>
                                <TextBlock Text="Страна *" 
                                   FontWeight="SemiBold"/>
                                <ComboBox x:Name="CountryComboBox"
                                          ItemsSource="{Binding Countries}"
                                          DisplayMemberPath="CountryName"
                                          SelectedValue="{Binding CurrentAddress.CountryId}"
                                          SelectedValuePath="CountryId"
                                          SelectionChanged="CountryComboBox_SelectionChanged"/>

                                <TextBlock Text="Регион" 
                                           FontWeight="SemiBold"
                                           Margin="0,10,0,0"/>
                                <TextBox Text="Реализовать позже"
                                         IsEnabled="False"
                                         Background="#F8F9FA"/>

                                <TextBlock Text="Город *" 
                                           FontWeight="SemiBold"
                                           Margin="0,10,0,0"/>
                                <ComboBox ItemsSource="{Binding Cities}"
                                          DisplayMemberPath="CityName"
                                          SelectedValue="{Binding CurrentAddress.CityId}"
                                          SelectedValuePath="CityId"/>

                                <TextBlock Text="Населенный пункт" 
                                           FontWeight="SemiBold"
                                           Margin="0,10,0,0"/>
                                <TextBox Text="Реализовать позже"
                                         IsEnabled="False"
                                         Background="#F8F9FA"/>

                                <TextBlock Text="Улица" 
                                           FontWeight="SemiBold"
                                           Margin="0,10,0,0"/>
                                <TextBox Text="Реализовать позже"
                                         IsEnabled="False"
                                         Background="#F8F9FA"/>

                                <TextBlock Text="Детали адреса" 
                                           FontWeight="SemiBold"
                                           FontSize="14"
                                           Margin="0,20,0,10"/>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                        <TextBlock Text="Номер дома *" 
                                                   FontWeight="SemiBold"/>
                                        <TextBox Text="{Binding CurrentAddress.HouseNumber, UpdateSourceTrigger=PropertyChanged}"
                                                 MaxLength="20"/>
                                        <TextBlock Text="Пример: 12, 12А, 12/2" 
                                                   Foreground="#666"
                                                   FontSize="11"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="Подъезд" 
                                                   FontWeight="SemiBold"/>
                                        <TextBox Text="{Binding CurrentAddress.EntranceNumber, UpdateSourceTrigger=PropertyChanged}"
                                                 MaxLength="10"/>
                                        <TextBlock Text="Пример: 1, 2, 3" 
                                                   Foreground="#666"
                                                   FontSize="11"/>
                                    </StackPanel>
                                </Grid>

                                <Border Background="#F8F9FA" 
                                        BorderBrush="#E0E0E0" 
                                        BorderThickness="1" 
                                        CornerRadius="4"
                                        Padding="10"
                                        Margin="0,20,0,0">
                                    <StackPanel>
                                        <TextBlock Text="📋 Предпросмотр адреса" 
                                                   FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding CurrentAddress.AddressView}"
                                                   Foreground="#666"
                                                   FontStyle="Italic"
                                                   TextWrapping="Wrap"
                                                   Margin="0,5,0,0"
                                                   MinHeight="40"/>
                                    </StackPanel>
                                </Border>

                                <StackPanel Orientation="Horizontal" 
                                            HorizontalAlignment="Right"
                                            Margin="0,30,0,0">
                                    <Button Content="Отмена" 
                                            Command="{Binding CancelCommand}"
                                            Background="#6c757d"
                                            Foreground="White"/>

                                    <Button Content="{Binding SaveButtonText}" 
                                            Command="{Binding SaveAddressCommand}"
                                            Background="#28a745"
                                            Foreground="White"
                                            Margin="10,0,0,0"/>
                                </StackPanel>

                                <Border Background="#FFEBEE" 
                                        BorderBrush="#EF9A9A" 
                                        BorderThickness="1" 
                                        CornerRadius="4"
                                        Padding="10"
                                        Margin="0,20,0,0"
                                        Visibility="{Binding HasErrors, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <TextBlock Text="{Binding ErrorMessage}" 
                                               Foreground="#D32F2F" 
                                               TextWrapping="Wrap"/>
                                </Border>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </Border>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>